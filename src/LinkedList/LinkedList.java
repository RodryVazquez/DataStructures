package LinkedList;

public class LinkedList {

    private Node first;
    private Node last;

    /*
     * Look up by value O(N)
     * Look up by index O(N)
     * */
    //addFirst, add first have O(1) complexity
    public void addFirst() {
    }

    public void addLast() {
    }

    public int indexOf() {
        return -1;
    }
}
